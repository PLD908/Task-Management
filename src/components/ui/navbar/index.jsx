import React, { useContext } from 'react'

const Navbar = () => {

    return (
        <nav
            style={{
                background: "green",
                textAlign: "center",
            }}>
            <h1>NAVBAR</h1>
        </nav>
    );  
};

export default Navbar